import { TypeMaterialLink } from './type-material-link.model';

describe('TypeMaterialLink', () => {
  it('should create an instance', () => {
    expect(new TypeMaterialLink()).toBeTruthy();
  });
});
