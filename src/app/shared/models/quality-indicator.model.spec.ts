import { QualityIndicator } from './quality-indicator.model';

describe('QualityIndicator', () => {
  it('should create an instance', () => {
    expect(new QualityIndicator()).toBeTruthy();
  });
});
