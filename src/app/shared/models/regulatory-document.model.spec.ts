import { RegulatoryDocument } from './regulatory-document.model';

describe('RegulatoryDocument', () => {
  it('should create an instance', () => {
    expect(new RegulatoryDocument()).toBeTruthy();
  });
});
