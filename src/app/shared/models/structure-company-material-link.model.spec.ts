import { StructureCompanyMaterialLink } from './structure-company-material-link.model';

describe('StructureCompanyMaterialLink', () => {
  it('should create an instance', () => {
    expect(new StructureCompanyMaterialLink()).toBeTruthy();
  });
});
