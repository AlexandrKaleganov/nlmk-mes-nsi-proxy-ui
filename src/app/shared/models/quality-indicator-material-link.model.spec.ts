import { QualityIndicatorMaterialLink } from './quality-indicator-material-link.model';

describe('QualityIndicatorMaterialLink', () => {
  it('should create an instance', () => {
    expect(new QualityIndicatorMaterialLink()).toBeTruthy();
  });
});
