import { ClassMaterialLink } from './class-material-link.model';

describe('ClassMaterialLink', () => {
  it('should create an instance', () => {
    expect(new ClassMaterialLink()).toBeTruthy();
  });
});
