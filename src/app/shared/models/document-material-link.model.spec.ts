import { DocumentMaterialLink } from './document-material-link.model';

describe('DocumentMaterialLink', () => {
  it('should create an instance', () => {
    expect(new DocumentMaterialLink()).toBeTruthy();
  });
});
